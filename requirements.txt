# Edge runtime — install on the Raspberry Pi / edge device
# Heavy ML packages (ultralytics, torch, torchvision) are in requirements-train.txt

flask>=3.0
opencv-python-headless>=4.9     # headless: no GUI deps, smaller on ARM
onnxruntime>=1.18               # ARM wheels available via pip for Raspberry Pi
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
